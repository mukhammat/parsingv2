{
  "name": "crm_bakhodur",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "pm2 start dist/index.js --name crm_bakhodur",
    "generate": "npx drizzle-kit generate",
    "migrate": "npx drizzle-kit migrate",
    "genmig": "npm run generate && npm run migrate"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cheerio": "^1.1.2",
    "dotenv": "^17.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "tsx": "^4.7.1",
    "typescript": "^5.8.3"
  }
}
